# React Native / Expo requires hoisted node_modules (pnpm strict isolation breaks hermes-compiler resolution)
# This is the standard recommendation for Expo projects with pnpm.
# See: https://docs.expo.dev/guides/using-pnpm/
shamefully-hoist=true
